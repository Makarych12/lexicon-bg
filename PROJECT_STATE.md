# PROJECT STATE — Тренажор на електрическата схема на тепловоз Серия 07 БДЖ (ТЭ109 «Людмила»)

## О проекте

Интерактивный тренажёр-симулятор электрической схемы пуска дизеля тепловоза
Серия 07 БДЖ (ТЭ109 «Людмила») на основе болгарского учебного пособия.
Single Page Application (HTML5/CSS3/Vanilla JS), работающая локально без
тяжёлых зависимостей.

**Цель:** Наглядное пошаговое обучение цепям пуска дизеля с анимацией токов,
физической анимацией контактов (ножи реле/контакторов) и информационными
карточками аппаратов, полностью на болгарском языке с опцией русского.

---

## Архитектура и стек

| Файл | Назначение | Размер |
|------|-----------|--------|
| `index.html` | Оболочка SPA: шапка, навигация режимов, панели, контроли шагов, подсказка | ~3 KB |
| `style.css` | Тёмная индустриальная тема, неоновые анимации токов, адаптивность | ~15 KB |
| `schema.svg` | Полная электрическая схема (9 реек, 60+ элементов, 40+ проводников) | ~28 KB |
| `app.js` | Вся логика: зум/пан, движок последовательностей, карточки, i18n, аварийный режим | ~76 KB |

### Технологии
- **HTML5** — семантическая разметка
- **CSS3** — кастомные свойства, Flexbox, CSS-анимации (pulse, spin, flow, pressspin)
- **Vanilla JS** — без фреймворков/библиотек
- **SVG** — встроенная векторная схема с программным управлением
- **HTTP** — локальный сервер Python HTTP (для загрузки `schema.svg` через fetch)

### Ключевые компоненты логики (app.js)

| Модуль | Описание |
|--------|---------|
| `DEVICE_INFO` | Словарь аппаратов (описание, назначение, клеммы, контакты) — двуязычный |
| `WIRE_INFO` | Словарь проводников (номера, описание цепей) — двуязычный |
| `STEPS` | Тексты шагов 0-4 (заголовок, описание) — двуязычный |
| `STEP_STATE` | Полные состояния каждого шага (провода, контакты, катушки, устройства, двигатели) |
| `SEQUENCES` | Последовательности анимации тока от + к – для каждого шага |
| `ANIM` | Движок — воспроизводит события с задержками, отслеживает duration |
| `CONTACTS` | Маппинг контактов для информационных карт |
| `EMERGENCY` | 6 типовых аварийных ситуаций с перемычками |
| Camera | Зум к курсору, панорамирование, smooth transition, fit-to-screen |
| i18n | Переключатель `RU`/`BG`, функция `lang()`, словарь `T` для UI-надписей |

---

## Что полностью реализовано

### 1. Интерактивная SVG-схема

- **Zoom & Pan:** колесо мыши (zoom to cursor), левая кнопка + drag, кнопки +/-/Reset View
- **Стартовый масштаб:** ~112%, центрирован на зону главных цепей (батарея, топливная/масляная помпы)
- **Все элементы кликабельны:** при клике на аппарат открывается информационная карточка (справа, плавный выезд)
- **Tooltip:** кастомная подсказка при наведении (берётся из DEVICE_INFO/WIRE_INFO, двуязычная)
- **Подписи проводов:** номера 138, 137, 191, 211, 224, 225, 341, 343, 379, 212, 213, 214, 131, С1, 410, 412, 254, 262

### 2. Пошаговая цепь пуска дизеля (5 шагов)

**Шаг 0 — Исходное состояние (подготовка)**
- Включаются: шалтер ВБ, СЗБ, автоматы А1, А2, А5, АУ (анимированные ножи)
- Реверсор в нейтрали, контакты РУ7 (н.з.) и РУ9 (н.з.) замкнуты
- Шины + и – под напряжением

**Шаг 1 — Включение КТН и топливного насоса**
- Ток: +АБ → 138 → ВБ → 137 → СЗБ → 191 → ШЗБ → 211 → А1 → 224 → А2 → 225/341/343/379 → **катушка КТН**
- Минус: через рейки 1М14, 1ТН1, реверсор, РУ7 (н.з.)
- КТН замыкает силовой/вспомогательный контакты → ток по 212/213/214 → **МТ (топливный насос) запущен**
- Анимация: последовательно от батареи к нагрузке, микрозадержка контактов после катушки

**Шаг 2 — Предварительная маслопрокачка (60 с)**
- Нажатие кнопки ПД (анимируется), замыкается РДМ3 → БПД → **катушка КМН**
- КМН → силовой контакт → **МН (маслопрокачка) запущен**
- КМН → вспомогательные контакты → **МР6** (блок-магнит) + **ТРВ** (таймер 60 с)
- Таймер отображается на схеме с обратным отсчётом (60 → 0)

**Шаг 3 — Пуск и прокрутка стартер-генератором**
- БПД (таймер) замыкает выход → ток через контакты КТН, КМН, РУ9 н.з., защиту 105
- **ВП7** (шток ускорителя выдвигается) + **катушка Д** (пусковой контактор)
- Д замыкает силовой контакт → мощный ток 131/С1 → **СтГ (стартер-генератор) вращается** как двигатель
- Возбудитель **СВ** питает БПД (410/412)

**Шаг 4 — Подхват дизеля и завершение пуска**
- При 180-220 об/мин замыкается **РДМ4** → тиристор БПД → **РУ9/РУ10**
- РУ9/РУ10 размыкают н.з. в пусковой цепи → **Д и ВП7 отпадают** (контактор Д отключается, СтГ прекращает работать как двигатель)
- РУ9/РУ10 замыкают н.о. → **КРН** → силовой контакт КРН → СтГ переходит в генераторный режим (110 В)
- Заряд батареи по проводам 262/254
- Топливный и масляный насосы продолжают работать

### 3. Физическая анимация контактов

- Все контакты имеют подвижный «нож» (blade), отрисовываемый SVG `<line>` с трансформацией
- Угол раскрытия: 38° (относительно горизонтали)
- Замыкание: `transform: rotate(0deg)` с плавным CSS transition
- Размыкание: обратная анимация
- Катушки реле/контакторов пульсируют при запитке (ключевой кадр `coilpulse` с уменьшением непрозрачности до 45%)
- Блок-контакты срабатывают с микрозадержкой после катушки (300-400 мс в последовательности)
- Шток ускорителя ВП7 выдвигается при запитке (CSS translateX + свечение)
- Кнопка ПД анимируется нажатием (утопление + пульсирующее кольцо)

### 4. Анимация токов (бегущие неоны)

- Активные провода: `stroke-dasharray: 12 16`, анимация `stroke-dashoffset` (0.85 с)
- Яркий неоново-жёлтый цвет (#ffb020) с фильтром свечения
- Номера проводов подсвечиваются золотым при активации
- Пассивные провода: тёмно-серые (#3f5064)
- При переходе между шагами старые провода гаснут (CSS transition), новые загораются последовательно

### 5. Информационные карточки и локализация

- **Болгарский** — язык по умолчанию (BG)
- **Русский** — опционально (кнопка RU)
- Двуязычные словари: DEVICE_INFO, WIRE_INFO, T (UI-надписи), STEPS (тексты шагов), EMERGENCY
- Информационная карточка: название, тип, клеммы, назначение, состояние контактов на текущем шаге, примечание
- Функция `lang(x)` — возвращает `x[LANG]` (объект) или `x` (строка)

### 6. Аварийный режим (Аварийные премички)

- 6 типовых неисправностей с описанием причины и местом установки перемычки
- Выпадающий список, динамическое отображение
- Двуязычный

### 7. Управление и UI

- **Кнопки:** «Предишна стъпка», «Следваща стъпка», «Автовъзпроизвеждане» (с последовательной анимацией)
- **Прогресс-бар** шагов
- **Шуфляда (drawer):** описание текущего этапа на болгарском
- **Легенда:** условные обозначения (активная цепь, запитан апарат, замкнут контакт, вращающийся механизм)
- **Тост:** уведомления (нереализованные режимы, ошибки)
- **Адаптивность:** медиа-запрос для узких экранов (< 900px)

---

## План следующих шагов для следующего агента

### 1. Схема возбуждения тягового генератора (Празен ход / Тяга)

**Аппараты:** КВ (контактор возбуждения), ВВ (возбудитель), РУ5 (реле управления), РВ3, П1-П6 (позиции контроллера)

**Цепь:** +АБ → ВВ → КВ → обмотка возбуждения тягового генерара → РУ5 → РВ3 → минус. Позиции П1-П6 задают ступени возбуждения.

**Что нужно сделать:**
- Добавить SVG-элементы для КВ, ВВ, РУ5, РВ3, обмотки ГГ
- Добавить провода с номерами
- Добавить DEVICE_INFO для новых аппаратов
- Добавить STEPS для режима «Празен ход» и «Тяга»
- Реализовать SEQUENCES для анимации тока через контакты КВ, резисторы РВ3
- Визуализировать рост тока возбуждения при переводе КМ (П1→П6)

### 2. Схема ослабления поля ТЭД (ВШ1, ВШ2)

**Аппараты:** ВШ1, ВШ2 (вентили шунтировки), РП1, РП2 (реле перехода), СШ1-СШ6 (резисторы)

**Цепь:** Параллельно обмоткам возбуждения ТЭД подключаются резисторы СШ1-СШ6 через вентили ВШ1/ВШ2 (двухступенчатое ОП).

**Что нужно сделать:**
- Добавить SVG-элементы для ВШ1, ВШ2, РП1, РП2, СШ1-СШ6
- Провода с номерами
- Логика переключения: при скорости > 40 км/ч включается ВШ1, > 60 км/ч — ВШ2
- Анимация тока шунтировки
- Новый режим в навигации

### 3. Защиты и аварийные перемычки

**Аппараты:** РЗ (реле заземления), РМ1, РМ2 (реле максимального тока), РБ1-РБ3 (реле боксования), КАВ/АП (аварийное возбуждение)

**Схема:** Анимация срабатывания защиты, отключение соответствующих цепей, переход в аварийный режим.

**Что нужно сделать:**
- Добавить SVG-элементы для РЗ, РМ1, РМ2, РБ1-РБ3, КАВ, АП
- Реализовать сценарии: короткое замыкание (РМ1/РМ2), боксование (РБ1-РБ3), пробой на землю (РЗ)
- Аварийные перемычки: КАВ (шунтирует ВВ), аварийное возбуждение от АБ
- Интегрировать с существующим аварийным режимом

### 4. Улучшения (желательно)

- **Touch-события:** поддержка жестов (pinch zoom, swipe) для мобильных устройств
- **Сохранение состояния:** сохранение текущего шага и языка в localStorage
- **Экспорт/печать:** кнопка для печати схемы с текущим состоянием
- **Тесты:** unit-тесты для `STEP_STATE` и `SEQUENCES` (Node.js с jsdom)

---

## Инструкция по запуску

### Требования
- Python 3 (встроенный HTTP-сервер) или Node.js (http-server, serve)

### Запуск локального сервера

```bash
cd /path/to/my-vibe-project

# Вариант 1: Python 3
python3 -m http.server 8080 --bind 127.0.0.1

# Вариант 2: Node.js (если установлен serve)
npx serve -l 8080 .

# Вариант 3: Node.js (http-server)
npx http-server -p 8080 .
```

### Открытие в браузере

```
http://127.0.0.1:8080
```

### Проверка

1. Выберите режим **«Пускане на дизела»** (активен по умолчанию)
2. Нажимайте **«Следваща стъпка»** — наблюдайте последовательную анимацию тока
3. Используйте **«Автовъзпроизвеждане»** для автоматического прохождения
4. Кликайте на любой аппарат — открывается информационная карточка
5. Колесо мыши — зум, перетаскивание — панорамирование
6. Кнопка **BG/RU** — переключение языка

---

## Файловая структура

```
my-vibe-project/
├── index.html        # SPA оболочка
├── style.css         # Стили и анимации
├── app.js            # Вся логика приложения
├── schema.svg        # Векторная схема
└── PROJECT_STATE.md  # Данный файл
```

---

*Дата фиксации: 2026-08-17*
*Следующему агенту: начать с раздела «План следующих шагов», пункт 1 — «Схема возбуждения тягового генератора».*