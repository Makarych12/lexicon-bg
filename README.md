# Glagolica

Интерактивные карточки для изучения болгарской и словенской лексики: глаголы, спряжения, времена и грамматическая викторина.

Приложение помогает запоминать болгарские глаголы вместе с переводом, произношением (IPA) и примерами употребления во всех временах, а также проверяет знания через викторину по грамматике.

## Словенский блок

Переключатель «Болгарский / Словенский» в шапке открывает отдельный курс словенского языка по уровням A1–C1 (335 глаголов, ≈2800 примеров):

- вид глагола (dovršni / nedovršni / dvovidski), группа спряжения и чередования в основе;
- спряжение в единственном, двойственном (dvojina) и множественном числе в шести формах: sedanjik, preteklik, predpreteklik, prihodnjik, pogojnik, velelnik — с подсветкой окончаний и подсказкой-правилом;
- 6–11 контекстных примеров на глагол (словенский + русский, подсвеченная форма), озвучка через Web Speech API (`sl-SI`);
- у каждой карточки своя фотография с [Pexels](https://www.pexels.com) (подобрана по смыслу глагола, уникальна для каждой карточки; при недоступности картинки показывается emoji-сцена);
- 3D-карточки с вкладками «Обзор / Спряжение / Примеры», фильтры по теме и виду, поиск, тёмная и светлая тема, трекер выученных глаголов (localStorage);
- викторина с вопросами по формам (включая двойственное число), виду, переводу и пропускам в предложении, с разбором ошибок в конце;
- раздел «Алфавит и произношение»: 25 букв и сочетания lj, nj, dž в виде карточек с IPA, русским соответствием, примером и озвучкой, плюс шесть правил чтения.

Данные словенских глаголов хранятся в `index.html` компактными строками-сидами (`slSeeds`), из которых формы и примеры строятся генератором на клиенте.

## Сравнение языков

Раздел «BG ↔ SL»: одно значение — болгарское и словенское слово рядом, 222 пары. 69 из них подтянуты автоматически из обеих колод по совпадению перевода (у таких работает переход на карточку), остальные 153 добавлены мостом `slBgBridge` — базовая лексика, которой в болгарской колоде нет. У двенадцати пар есть заметка: ложные друзья (`mesto` — город, а не «место»), двойные значения (`teči` — и «бежать», и «течь»; `dolg` — и «длинный», и «долг») и расхождения корней. Фильтры по части речи, уровню и «только с заметкой»; озвучка с правильным языком у каждой колонки.

## Разговорник и грамматика

- **Разговорник** — 48 ходовых фраз A1–A2 в шести темах (приветствие, вежливость, кафе, магазин, дорога, время), каждая сразу на трёх языках с озвучкой болгарского и словенского.
- **Грамматический справочник** — десять карточек: шесть падежей, двойственное число, пять типов склонения, одушевлённость и мягкие основы, прилагательное, вид глагола, времена, клитики и порядок слов, отрицание, чтение и ударение. Везде отмечено, где болгарский устроен иначе.

## Избранное, клавиши, ссылки и офлайн

- **Избранное** — ☆ на карточке в обоих блоках, отдельный фильтр; в словенском избранное показывается сразу со всех уровней. Хранится в `localStorage` раздельно по языкам.
- **Горячие клавиши** — в болгарской ленте: `←` `→` листать, пробел перевернуть, `1` не знаю, `2` знаю, `F` в избранное. В разделе повторения: пробел показать ответ, `1` / `2` — оценка.
- **Ссылка на карточку** — кнопка ↗ копирует адрес вида `#sl/verb/delati`, `#sl/word/n-hiša`, `#bg/card/12`, `#bg/verb/3`; по такой ссылке сайт сам открывает нужный блок, уровень и карточку.
- **PWA и офлайн** — `manifest.webmanifest`, `icon.svg` и `sw.js`: сайт ставится на телефон как приложение, оболочка кэшируется при установке, фотографии — по мере просмотра (последние 400). Проверено с отключённой сетью: страница открывается из кэша, данные и уже виденные фотографии на месте.

## Существительные и прилагательные

Отдельный раздел словенского блока: 48 существительных и 24 прилагательных, уровни A1–B2, у каждого — своя фотография. Формы не хранятся, а строятся генератором из основы и типа склонения, поэтому набор расширяется одной строкой-сидом.

- существительное — шесть падежей в трёх числах (ednina, dvojina, množina), пять типов склонения: мужской на согласный, женский на -a, женский i-основы, средний на -o и на -e; учитывается одушевлённость (T. ed. = R. ed.), мягкие основы (`konj → konjem`, `konjev`) и чередование в основе (`pes → psa`);
- прилагательное — три рода × три числа × шесть падежей, с переключением числа;
- окончание в таблице подсвечено; если основа чередуется, подсветки нет — это видно сразу;
- тот же режим «напиши сам» и те же оценки для интервальных повторений, что и у глаголов.

Проверено на эталонных парадигмах: `hiša / hiše / hiši / hišo`, `noč → nočjo, nočmi`, `polje → poljem`, `vroč → vroče`, `dober → dobrega`.

## Игровые режимы

Раздел «Игры» работает на всей словенской базе — 407 единиц (глаголы плюс существительные и прилагательные), с фильтром по уровню. Рекорды лежат в `localStorage`; на интервальные повторения игры не влияют, это разминка, а не проверка.

- **Match** — шесть пар «слово ↔ перевод» на время, неверная пара подсвечивается и сбрасывается, лучшее время запоминается по уровню;
- **Спринт** — минута на максимум правильных ответов, рекорд по уровню;
- **Определение уровня** — десять вопросов по два с каждого уровня; результат предлагает, с чего начать, и открывает этот уровень. При первом заходе в словенский блок над сеткой появляется ненавязчивая подсказка про этот тест — её можно скрыть насовсем.

## Интервальные повторения и режим «напиши сам»

Оба блока держат свой прогресс повторений в `localStorage` (`lexiconSrsBg`, `lexiconSrsSl`) — по упрощённому SM-2. После показа карточки есть две оценки: «Не знаю» сбрасывает серию и возвращает карточку минут через десять, то есть ещё в этой сессии; «Знаю» отодвигает её на день, затем на три, дальше интервал умножается на коэффициент лёгкости (он растёт с каждым верным ответом и падает с каждым провалом, в пределах 1.3–2.8).

- **Словенский**: раздел «Повторение» — одна карточка за раз, до 20 за заход, с фильтром по уровню и счётчиками «к повторению / новых / в работе / закреплено». Ответ скрыт, пока не нажмёшь «Показать ответ». Оценить глагол можно и из сетки — на вкладке «Обзор» карточки.
- **Болгарский**: кнопка «⟳ Повторение» рядом с поиском оставляет в ленте только подошедшие по сроку карточки, самые просроченные первыми; оценки — на обороте карточки.

Режим **«Напиши сам»** переключает таблицу спряжения на поля ввода: формы набираются по памяти, «Проверить» сверяет каждую и подсвечивает в правильном ответе ту часть, с которой ответ разошёлся, — обычно это окончание. Ответ без диакритики (`pisem` вместо `pišem`) засчитывается как «почти» с подсказкой, чего не хватает. Итог проверки уходит в интервальные повторения: ≥80% верных — «знаю», иначе «не знаю». Есть и в словенской таблице (все шесть времён, три числа), и в болгарском «Сегашно време».

## Фотографии

У каждой карточки — своя фотография с [Pexels](https://www.pexels.com), подобранная по смыслу слова: 335 словенских глаголов, 109 болгарских слов и 121 болгарский глагол, все кадры разные. Подписи фотографов на карточках не выводятся (лицензия Pexels атрибуции не требует), авторство хранится в данных `slPhotos`, `bgNounPhotos` и `bgVerbPhotos`. Картинки берутся в трёх готовых размерах, которые Pexels отдаёт сам (`medium` ~525px, `large` 940px, `large2x` 1880px), и раздаются через `srcset`/`sizes`: на телефоне не тянется лишнее, на Retina кадр остаётся резким. Произвольные `w`/`h` так не годятся — их CDN пересобирает кадр на лету и каждый запрос идёт мимо кеша: замер на десяти наших фотографиях дал медиану 460 мс против 211 мс у `medium`, причём байт было больше. Первый кадр прописан прямо в разметке и в `<link rel=preload>`, поэтому загрузка начинается во время разбора HTML, а не после выполнения скрипта; соседние карточки греются заранее тем же вариантом, который выберет браузер. Пока кадр летит, на его месте мягкое мерцание, а не пустой прямоугольник.

## Производительность

Tailwind собран заранее и встроен в `<style>`: CDN-скрипт `cdn.tailwindcss.com` компилировал CSS прямо в браузере. На мобильном профиле с четырёхкратным замедлением CPU это дало DOMContentLoaded 1353 → 455 мс, время выполнения скриптов 443 → 189 мс, память JS 15 → 4 МБ. Плюс: поиск по глаголам перестраивает сетку один раз после паузы, сетка не перерисовывается при неизменном наборе карточек, карточки за пределами экрана не рисуются (`content-visibility`), а эффекты наведения и всплывающие подсказки включаются только при наличии мыши — на телефоне они «залипали» после тапа и растягивали страницу вбок.

## Живая версия

https://lexicon-bg.vercel.app/

## Технологии

Статический сайт без сборки и зависимостей — весь код (разметка, стили, логика и данные по глаголам) находится в одном файле `index.html`. Внешних подключений нет: Tailwind встроен готовым CSS, шрифты системные, снаружи грузятся только фотографии с Pexels.

## Запуск локально

Открыть `index.html` в браузере, либо поднять любой статический HTTP-сервер в корне проекта, например:

```bash
python3 -m http.server 8000
```

и перейти на `http://localhost:8000/`.

## Деплой

Проект развёрнут на [Vercel](https://vercel.com) (конфигурация — `vercel.json`); поддерживается автоматически и через GitHub Pages.
